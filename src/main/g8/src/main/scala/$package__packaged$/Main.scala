package $package$

import $package$.$name;format="Camel,word"$Caos
import $package$.$name;format="Camel,word"$
import caos.frontend.Configurator
import caos.frontend.Site.initSite

/** Main class 
 *
 * Calls CAOS site generator with a custom configuration
 */
object Main {
  def main(args: Array[String]):Unit = {
    initSite[$name;format="Camel,word"$]($name;format="Camel,word"$Caos)
  }
}
